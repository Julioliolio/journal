import { DemoCanvas } from "@/components/DemoCanvas";

export const dynamic = "force-dynamic";
export const metadata = { title: "journal — demo" };

export default function DemoPage() {
  return <DemoCanvas />;
}
